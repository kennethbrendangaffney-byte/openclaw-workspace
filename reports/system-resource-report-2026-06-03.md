# 📊 SYSTEM RESOURCE REPORT — June 3, 2026

Generated by Karen for Ken's de-clutter day.

---

## 💾 DISK USAGE — Full Breakdown

### Physical Disk Layout
| Device | Size | Partition | Mount | Notes |
|--------|------|-----------|-------|-------|
| **nvme0n1** (CT1000E100SSD8) | **931.5 GB** | — | — | Full 1TB SSD |
| nvme0n1p1 | 100 MB | EFI | /boot/efi | Boot partition |
| nvme0n1p2 | 16 MB | — | — | Reserved (likely MSR) |
| nvme0n1p3 | 1,000 MB | — | — | Recovery/Windows |
| **nvme0n1p4** | **810.4 GB** | — | — | **Unmounted — Windows partition?** |
| **nvme0n1p5** | **120 GB** | Linux | / | **Current Ubuntu root** |

### Root Partition (/) — 118 GB Total
| Metric | Value |
|--------|-------|
| **Total** | 118 GB |
| **Used** | 56 GB (50%) |
| **Available** | 57 GB |
| **Status** | Healthy — no immediate pressure |

---

## 🔍 WHERE THE 56 GB IS GOING — Top-Level Breakdown

| Location | Size | % of Used | Category |
|----------|------|-----------|----------|
| `/usr` | **12.3 GB** | 22% | System + apps |
| `/home/karen` | **22 GB** | 39% | User data |
| `/var` | **9.0 GB** | 16% | Logs, cache, packages |
| `/home/karen/.openclaw` | **4.4 GB** | 8% | Agent workspace |
| Other system | ~8 GB | 15% | Boot, tmp, etc. |

---

## 🏠 /home/karen — 22 GB Deep Dive

### Visible Folders
| Folder | Size | Notes |
|--------|------|-------|
| `snap/` | **2.1 GB** | Snap app data (Discord, Firefox, etc.) |
| `piper/` | **584 MB** | TTS voice models — still needed? |
| `Documents/` | **227 MB** | User docs |
| `BitNet/` | **223 MB** | **DEAD — models removed, binaries remain** |
| `Downloads/` | **199 MB** | Downloads folder |
| `sunshine.AppImage` | **41 MB** | Game streaming app |

### Hidden Folders (dotfiles) — THE BIG HITTERS
| Folder | Size | Notes |
|--------|------|-------|
| `.local/lib/python3.12/site-packages` | **5.1 GB** | **#1 SPACE HOG** — Python packages |
| `.config/google-chrome` | **4.4 GB** | **#2 SPACE HOG** — Chrome data |
| `.openclaw` | **4.4 GB** | Agent workspace |
| `.npm` | **1.6 GB** | Node.js packages |
| `.cache` | **971 MB** | General cache |
| `.venvs` | **652 MB** | Python virtual environments |
| `.npm-global` | **563 MB** | Global npm packages (includes OpenClaw) |
| `.kimi-code` | **130 MB** | Kimi Code IDE data |
| `.paperclip` | **75 MB** | Paperclip app data |

---

## 🐍 Python Packages — 5.1 GB Breakdown

| Package/Folder | Size | Notes |
|----------------|------|-------|
| `nvidia/` | **2.7 GB** | CUDA libs — **no GPU on this machine** |
| `torch/` | **1.2 GB** | PyTorch |
| `triton/` | **641 MB** | GPU compiler — **no GPU** |
| `transformers/` | **97 MB** | Hugging Face |
| `ctranslate2` + libs | **135 MB** | Translation/inference |
| `sympy` | **74 MB** | Symbolic math |
| `av` + libs | **105 MB** | Audio/video |
| `onnxruntime` | **55 MB** | ML runtime |
| `numpy` + libs | **69 MB** | Numerical computing |
| `sherpa_onnx` + libs | **91 MB** | Speech recognition |
| `networkx` | **18 MB** | Graph theory |
| `tokenizers` | **11 MB** | Text tokenization |
| `faster_whisper` | **1.5 MB** | Whisper STT |
| Others | ~200 MB | Various utilities |

**⚠️ KEY FINDING:** `nvidia/` (2.7 GB) and `triton/` (641 MB) are GPU-only packages on a machine with **integrated Radeon graphics, no CUDA**. These are dead weight.

---

## 🌐 Chrome — 4.4 GB Breakdown

| Folder | Size | Notes |
|--------|------|-------|
| `OptGuideOnDeviceModel` | **4.0 GB** | **MASSIVE** — On-device AI model cache |
| `OptGuideOnDeviceClassifierModel` | **121 MB** | Classifier model |
| `optimization_guide_model_store` | **110 MB** | Model store |
| `Default/` (profile) | **71 MB** | Actual browsing data |
| `component_crx_cache` | **35 MB** | Extension cache |
| `WasmTtsEngine` | **23 MB** | Text-to-speech engine |
| `Safe Browsing` | **22 MB** | Security data |
| Other caches | ~50 MB | Various |

**⚠️ KEY FINDING:** `OptGuideOnDeviceModel` at **4.0 GB** is Chrome's on-device AI (Gemini Nano/OptGuide). This is optional and can be cleared.

---

## 🤖 OpenClaw Workspace — 4.4 GB Breakdown

| Folder | Size | Notes |
|--------|------|-------|
| `agents/main/sessions/` | **2.3 GB** | Session history files |
| `workspace/memory/archive/` | **1.0 GB** | Archived daily notes (tar.gz) |
| `archive/sessions/2026-05/` | **313 MB** | Archived session backups |
| `media/inbound/` | **132 MB** | Received images/media |
| `memory/main.sqlite` | **100 MB** | Semantic memory database |
| `workspace/~` | **89 MB** | Unknown — needs checking |
| `workspace/memorized_media/` | **25 MB** | Cached media |
| `workspace/dashboard-v2/` | **21 MB** | Dashboard app |
| `delivery-queue/` | **26 MB** | Message queue |
| `tasks/` | **22 MB** | Task files |
| `workspace/downloads/` | **336 KB** | Downloaded files |
| Config backups | ~500 KB | Multiple `.clobbered` files |

**⚠️ SESSION FILES:** Multiple `.deleted.1777...` and `.checkpoint` files in `agents/main/sessions/` — these are orphaned cleanup files that weren't fully removed.

---

## 📦 System-Level Space Hogs

### /usr — 12.3 GB
| Location | Size | Notes |
|----------|------|-------|
| `/usr/local/lib/ollama` | **5.9 GB** | **Ollama binaries + ROCm/CUDA backends** |
| `/usr/lib` | **4.1 GB** | System libraries |
| `/usr/share` | **2.2 GB** | Shared data |
| `/usr/bin` | **493 MB** | Executables |

**Ollama backends:**
- `rocm/` — 2.5 GB (AMD GPU — **relevant**)
- `cuda_v12/` — 2.5 GB (NVIDIA — **no CUDA on this machine**)
- `cuda_v13/` — 953 MB (NVIDIA — **no CUDA**)
- `vulkan/` — 55 MB (Universal — relevant)

### /var — 9.0 GB
| Location | Size | Notes |
|----------|------|-------|
| `/var/lib/snapd/snaps/` | **3.7 GB** | Snap package files |
| `/var/log/` | **3.2 GB** | **LOG FILES** |
| `/var/cache/apt/archives/` | **595 MB** | Cached .deb packages |
| `/var/cache/` (other) | **167 MB** | Various caches |

**Log files detail:**
- `journal/` — 2.1 GB (systemd journal)
- `syslog.1` — 374 MB
- `kern.log.1` — 316 MB
- `syslog` — 180 MB (current)
- `kern.log` — 153 MB (current)
- Older compressed logs — ~50 MB

**APT cache detail:**
- 3x Google Chrome .deb files — 374 MB total
- 3x Tailscale .deb files — 108 MB total
- Linux kernel packages — ~100 MB
- Various others — ~15 MB

---

## 🧠 RAM USAGE — Full Breakdown

### Physical Memory
| Spec | Value |
|------|-------|
| **Total RAM** | **19.3 GB usable** (20.2 GB physical, 24 GB soldered LPDDR5) |
| **Used** | **3.4 GB** |
| **Free** | **11.2 GB** |
| **Buffers/Cache** | **5.2 GB** |
| **Available** | **15.9 GB** |
| **Swap Total** | **8.2 GB** (zram0) |
| **Swap Used** | **0 B** |

### Memory State
- **Usage:** 18% of total RAM actively used
- **Cache:** 27% of RAM is disk cache (will free automatically if needed)
- **Available:** 82% of RAM is effectively free for new processes
- **Swap:** Completely unused — system is not under memory pressure

### Top RAM Consumers (by RSS — resident set size)
| Process | PID | RAM | %CPU | Notes |
|---------|-----|-----|------|-------|
| Discord renderer | 10677 | **481 MB** | 18% | Discord app (active) |
| GNOME Shell | 3430 | **412 MB** | 4.3% | Desktop environment |
| **OpenClaw gateway** | **10219** | **412 MB** | 8.5% | **Your AI agent** |
| Perplexity renderer | 12423 | **281 MB** | 4.9% | Perplexity app |
| Perplexity zygote | 12334 | **275 MB** | 1.3% | Perplexity helper |
| Perplexity main | 12190 | **269 MB** | 1.0% | Perplexity app |
| Discord main | 10376 | **208 MB** | 1.6% | Discord app |
| Discord zygote | 10560 | **206 MB** | 5.2% | Discord helper |
| openclaw-logs | 10340 | **165 MB** | 0.7% | Log viewer |
| Sunshine (game stream) | 3680 | **143 MB** | 0.1% | Remote desktop |
| Xwayland | 4604 | **119 MB** | 0.5% | X11 compatibility |
| GNOME ding extension | 4265 | **102 MB** | 0.0% | Desktop icons |
| Mutter frames | 4661 | **94 MB** | 0.0% | Window manager |
| GSD xsettings | 4626 | **90 MB** | 0.0% | Settings daemon |

### CPU Usage (Top Consumers)
| Process | %CPU | Notes |
|---------|------|-------|
| Discord renderer | **18%** | Active chat/voice |
| OpenClaw gateway | **8.5%** | Processing your messages |
| Discord zygote | **5.2%** | Background Discord work |
| Perplexity renderer | **4.9%** | Active AI chat |
| GNOME Shell | **4.3%** | Desktop compositing |

---

## 🎯 DE-CLUTTER RECOMMENDATIONS — Prioritized by Impact

### 🔴 HIGH IMPACT (1 GB+ each)

| # | Action | Space Freed | Risk | Notes |
|---|--------|-------------|------|-------|
| 1 | **Clear Chrome `OptGuideOnDeviceModel`** | **~4.0 GB** | Low | Chrome's on-device AI. Go to `chrome://settings/ai` → disable "Help me write" and other AI features, then delete the folder |
| 2 | **Remove Python `nvidia/` packages** | **~2.7 GB** | Medium | `pip uninstall nvidia-cublas-cu12 nvidia-cuda-runtime-cu12 nvidia-cudnn-cu12` etc. Check what's actually needed first |
| 3 | **Remove Python `triton/`** | **~641 MB** | Medium | GPU compiler, no GPU on this machine. `pip uninstall triton` |
| 4 | **Clean Ollama CUDA backends** | **~3.5 GB** | Medium | Remove `cuda_v12/` and `cuda_v13/` from `/usr/local/lib/ollama/`. Keep `rocm/` and `vulkan/` |
| 5 | **Vacuum systemd journal** | **~1.5 GB** | Low | `sudo journalctl --vacuum-size=500M` |
| 6 | **Clear old logs** | **~500 MB** | Low | `sudo logrotate -f /etc/logrotate.conf` or manually compress old logs |

### 🟡 MEDIUM IMPACT (100 MB — 1 GB)

| # | Action | Space Freed | Risk | Notes |
|---|--------|-------------|------|-------|
| 7 | **Clean APT cache** | **~595 MB** | Low | `sudo apt clean` — removes all cached .deb files |
| 8 | **Archive old session files** | **~500 MB** | Low | Move `.deleted.*` and old `.checkpoint` files from `.openclaw/agents/main/sessions/` |
| 9 | **Review `piper/` voice models** | **~584 MB** | Low | Keep only voices you use. TTS is currently off |
| 10 | **Clean `BitNet/` folder** | **~223 MB** | Low | Models already removed, just delete remaining binaries |
| 11 | **Compress old memory archives** | **~200 MB** | Low | Some `.tar.gz` files could be moved to external storage |
| 12 | **Clean `workspace/~` folder** | **~89 MB** | Low | Check what's in there first |

### 🟢 LOW IMPACT (< 100 MB)

| # | Action | Space Freed | Notes |
|---|--------|-------------|-------|
| 13 | Remove old OpenClaw config backups | ~200 KB | Multiple `.clobbered.*` files |
| 14 | Clean `Downloads/` | ~199 MB | Review and delete old downloads |
| 15 | Empty Trash | ~1.4 MB | `~/.local/share/Trash/` |

---

## 📈 POTENTIAL TOTAL SAVINGS

| Scenario | Space Freed | New Free Space |
|----------|-------------|----------------|
| **Conservative** (logs, cache, safe removals) | **~3 GB** | ~60 GB free |
| **Aggressive** (all recommendations above) | **~13 GB** | ~70 GB free |
| **Nuclear** (remove all Python ML libs, Ollama) | **~15 GB** | ~72 GB free |

**Note:** The 810 GB unmounted partition (nvme0n1p4) is likely your Windows partition. If you're not dual-booting anymore, that's a massive reclaim opportunity — but **ask first** before touching it.

---

## 🦞 KAREN'S VERDICT

**Disk:** 50% used is fine, but there's definite fat to trim. The Chrome AI model (4 GB) and Python CUDA packages (3.3 GB) are the biggest wins — both are dead weight on this machine.

**RAM:** Healthy. 15.9 GB available, swap untouched. OpenClaw using 412 MB is reasonable for a gateway process. Discord + Perplexity + GNOME using ~2 GB combined is normal for an active desktop session.

**Bottom line:** You could free up 5-10 GB with low-risk cleanup, 13+ GB if you're aggressive. The machine isn't struggling, but de-cluttering would make it snappier and give you headroom.

Want me to execute any of these cleanups? I'll start with the safest ones (logs, cache, BitNet) and work up to the bigger ones.
