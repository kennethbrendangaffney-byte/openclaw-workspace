# 2026-04-28 — Discord Setup Success

## What Happened
- Ken wanted Discord integration for Karen
- Created Discord bot "Karen2.0" at https://discord.com/developers/applications
- Added bot to server and enabled Message Content Intent

## Issues Encountered
1. **Config format was wrong** — Discord uses `token` (not `botToken`) and `allowFrom` (not `dms` structure like Telegram)
2. **Gateway needed restart** after config changes to load Discord plugin
3. **Logs revealed the real error**: `channels.discord: invalid config: must NOT have additional properties`

## Working Config
```json
"discord": {
  "enabled": true,
  "token": "<bot token>",
  "dmPolicy": "open",
  "allowFrom": ["*"]
}
```

## Result
- ✅ Gateway shows `ready (3 plugins: discord, openclaw-web-search, telegram)`
- ✅ Bot logged in as `Karen2.0 (LinuxLocal)`
- ✅ DM test successful — Ken confirmed "were in"

## Notes
- Discord schema is NOT the same as Telegram's — `token` instead of `botToken`, `allowFrom` array for access control
- Message Content Intent must be enabled in Discord Developer Portal
