GitHub backup failed — authentication missing

**Time:** 2026-05-18 23:00 Europe/Dublin
**Task:** Cron-triggered git backup

**What happened:**
1. Committed 4 files (including memory/2026-05-18.md) — commit e5dae2b
2. Push rejected — remote has commits I don't have locally
3. Fetched and merged origin/master — created merge commit 5f0cebb
4. Push hangs, then fails with HTTP 401 (authentication required)

**Root cause:** No GitHub credentials configured. The repo uses HTTPS URL but there's no:
- `~/.git-credentials` file
- `credential.helper` set
- Token or password in the remote URL

**Current state:**
- Local: merge commit 5f0cebb (has all local + remote changes merged)
- Remote (origin/master): f0a5101 (behind local by 2 commits)
- 2 local commits need to be pushed

**Fix needed:** Ken needs to provide a GitHub personal access token (PAT) or configure authentication. Options:
1. `git remote set-url origin https://TOKEN@github.com/kennethbrendangaffney-byte/openclaw-workspace.git`
2. Set up `git credential-store` with PAT
3. Switch to SSH key auth

I will not attempt to guess or create credentials. Waiting for Ken.
